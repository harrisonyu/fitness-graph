!!!
%html
  %head
    %title FitnessGraph
    = stylesheet_link_tag    "application", "bootstrap", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %header.navbar.navbar-fixed-top
      .navbar-inner
        .container
          = link_to image_tag("logo.jpeg", id: "logo", class: "nav pull-left"), root_path
          %h1.nav.pull-left Fitness Graphs
          %nav
            %ul.nav.pull-right
              %li= link_to "Home", home_path
              %li= link_to "Help",    '#'
              - if current_user
                %li= link_to "Sign out", signout_path
              - else
                %li= link_to "Sign in", signin_path
    .container
      - if flash[:notice]
        %div#flash_notice
          =flash[:notice]
      - if flash[:warning]
        %div#flash_alert
          =flash[:warning]
      = yield